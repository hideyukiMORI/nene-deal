# 2026-07-17

Front-overhaul burst under 統合リナ (hub) direction: **5 PRs merged** — W-Layer, W-Spec (two parts), the A1 hooks→model codemod, and a committed visual-smoke harness. Lane 2's CSS structural axes (`no-unlayered-css`, `selector-max-specificity`) and Lane 3 (A1) are complete, with appearance proven pixel-identical across the design × theme × route matrix. Lane 1 (Wave G gate) stays parked on fleet tooling.

---

## Lane 2 — W-Layer (#133, issue #133)

- Wrapped all rules in `frontend/src/app/design/{styles,designs}.css` in `@layer legacy {}` and installed the canonical cascade header `@layer theme, base, vendor, legacy, components, utilities;` in the FSD theme entry `src/shared/ui/theme/index.css` (AM-8(a); spelling matches the nene2 starter).
- Appearance-preserving by construction: `git diff -w` = 2 lines/file (the `@layer legacy {` / `}` wrappers only) [measured]; compiled bundle layer order is `theme → base → vendor → legacy → components → utilities`, so app CSS keeps beating Preflight and the app uses no Tailwind utilities [measured].
- Resolves the source of the **440 `no-unlayered-css`** violations [measured: fleet CSS audit + stylelint].

---

## Lane 2 — W-Spec (#134 part 1, #136 part 2)

- **part 1 (#134):** dropped the redundant `html` type selector from `html[data-design='X'] …` (the `data-design` attribute only ever sits on `<html>`, so the match set is unchanged; specificity `0,2,1 → 0,2,0`). `selector-max-specificity` **125 → 78** [measured: stylelint]. **0-pixel** before/after diff across 8 shots [measured: pixelmatch].
- **part 2 (#136):** flattened the remaining **78 → 0** via a purpose-built postcss + postcss-selector-parser engine — minimal `:where()` down to exactly `0,2,0`, preserving the trailing weight (descendant chains, single compound + pseudo-element, type-mixed `X button:hover`, combinator / `:not` / `[attr]`) [measured]. Two-way verification: stylelint re-measure = 0 (no under-reduction) and **0-pixel diff across 12 shots** (login / board / deal-detail × calm/enterprise × light/dark; no over-reduction / cascade flip) [measured].
- **Tie ledger** (in the PR body, per hub requirement): the `:where()` transform makes two base-vs-`[data-design]`-override groups tie at `0,2,0` (`.forecast .stat .label` / `.val`), so their precedence now depends on source order (`designs.css` after `styles.css`). Recorded as a silent-flip risk for future reorders; the 0-pixel diff confirms the override still wins today [measured].

---

## Lane 3 — A1 hooks → model (#135)

- Ran the `nene2-standards@1.1.0` codemod `nene2-a1-hooks-to-model`: **9 moves** (5 page hooks + `use-kanban-dnd` + 3 co-located tests) from `features/<slice>/hooks/` to `model/` (FSD 01:99 / 05:916). `use-kanban-dnd` is included per the owner's ruling.
- Move-only: 9× `R100` renames + import-path rewrites in barrels/views, `import type` preserved [measured: git]. `npm run check` fully green [measured].
- **Deviation:** keeping the codemod as a devDep makes `knip` fail (`unused` until the stylelint wiring lands in Lane 1), so it was uninstalled after the one-shot run, leaving a move-only diff. hub promoted this npx-oneshot form to the fleet-recommended pattern [reported: hub].

---

## tools — visual-smoke harness (#137)

- Committed `frontend/tools/smoke/` (`visual-smoke.mjs`, `visual-diff.mjs`, `README.md`) — the before/after screenshot device that proved 0-pixel diffs above, now a reusable tool.
- knip-clean by **reference, not ignore**: `package.json` gains `smoke` / `smoke:diff` scripts + the `playwright`/`pixelmatch`/`pngjs` devDeps, and `knip.json` `project` includes `tools/**/*.mjs`, so knip tracks the imports as used [measured: `npm run check` green]. Intended for reuse by W3 clear/invoice and Lane 1 gate validation [reported: hub].

---

## Analysis contributions (beyond code)

- **Lane 1 blocker:** found that the payout PR #189 template is a zero-violation vanguard (raw stylelint, no baseline), so it does not fit deal's 577 violations; the nonzero path is O-6 per-file severity from the legacy-manifest, machine-generated (AM-10 forbids hand-listing, registry source-of-truth = fleet, G-7). hub ruled **A** (fleet owns manifest generation) [reported: hub].
- **Lane 3 pre-stage:** delivered a hooks inventory + edge cases to fleet, which shaped the codemod rules.

---

## Remaining / handoff

- **Lane 1 (Wave G gate)** — waiting on fleet's legacy-manifest tooling [reported: hub]. Agreed plan: wire stylelint + clear the 12-violation tail + arm the gate with an **empty baseline** (zero debt → a fully-green CSS repo like payout), all in one lane.
- **CSS violation tail = 12** [measured: `color-no-hex` 2 + `declaration-no-important` 1; audit/reported: `data-theme-selector-location` 9]. Corrected hub's "11" (the 1 `declaration-no-important` was missing). deal's 577 breakdown = 440 + 125 + 9 + 2 + 1.
- **A1 extract** (audit-export / settings — View-inline logic → model hook) — separate track, not codemod-able.

---

## 📊 Numbers

- PRs merged: **5** (#133–#137), each with `npm run check` green [measured].
- `selector-max-specificity`: **125 → 0** [measured: stylelint].
- Before/after screenshots at **0-pixel** diff: ~20 across the design × theme × route matrix [measured: pixelmatch].
- A1: **9** file moves, all `R100` (content-identical renames) [measured: git].

---

## Session 2 — A1 extract (audit-export / settings) (#140, PR #141, merged `c33f34a`)

Closes the remaining hand-written A1 track from Lane 3: the two feature slices that
still had no `model/` hook (`audit-export`, `settings`). Under hub direction, extracted
their View-inline logic into `model/use-{audit-export,settings}-page.ts` and aligned both
with the fleet container/presentational convention (the Page calls the hook and spreads
`{...page}` into a props-taking View — same shape as login/kanban/deal-detail/stage/user).

- Added `use-audit-export-page` (`from`/`to`/`pending`/`invalidRange`/`download`) and
  `use-settings-page` (`value = draft ?? loaded`/`save`/`MONTH_END`/null mapping); Views
  became `*ViewProps`-typed presentational components; barrels match login. **11**
  co-located tests added [measured].
- Appearance-neutral: `AuditExportView` JSX `return` unchanged (diff hunks end before
  `return (`); `SettingsView` return-body change is the two `disabled` expressions only,
  same-valued aliases (`loading = settings.isPending`, `saving || loading =
  update.isPending || settings.isPending`, `||` order preserved) [measured: git diff].
- `npm run check` fully green (type-check / lint / format / **test 66** / knip /
  build-storybook) [measured]; existing `download-audit-csv.test.ts` untouched and green.
- **Review note (hub):** the first PR-body evidence claim over-generalised ("both Views'
  return unchanged") and self-contradicted the `disabled` bullet; corrected before merge to
  "AuditExportView = return fully unchanged / SettingsView = the two `disabled` aliases,
  with a mapping table". hub formalised "enumerate every changed JSX expression rather than
  calling it *unchanged*" as the standing condition for self-merging this track [reported: hub].
- deal's A1 is now complete (move #135 + extract #141); no hand-written track remains.
