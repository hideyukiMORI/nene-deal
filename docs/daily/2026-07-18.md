# 2026-07-18

Park day under 統合リナ (hub) direction (Lane 1 still waiting on fleet tooling). Closed
several small park-time tasks: a read-only fleet#65 measurement seed, a CI hardening PR
(Phase 0-1b), and an environment-hazard fix — plus stood up the new standing article-material
supply. All Issue-driven; the one code PR was self-merged after hub reconciled branch
protection. (The A1 extract #141 landed 07-17 21:13 JST and is logged under 2026-07-17
session 2.)

---

## fleet#65 seed — components-allowlist measurement (read-only)

hub asked for deal's distinct-component-class seed (3rd repo after vault156 / invoice381).
Scratch-wired `@hideyukimori/nene2-standards@1.1.0` (`.stylelintrc.json` extends form,
`--no-save --no-package-lock`, payout PR#189 shape) and measured. **Read-only — nothing
fixed, config removed, repo left clean.**

- **Key finding:** `nene2/layer-components-allowlist` = **0** — deal has no `@layer
  components` block (W-Layer #133 wrapped all app CSS in `@layer legacy`), so the rule that
  governs deal is `layer-legacy-manifest-only` (**2** files: `styles.css` / `designs.css`),
  not the allowlist [measured: stylelint]. deal is structurally different from vault/invoice
  (which place classes in `@layer components`).
- Full RED tally (exit 2, 14 total): legacy-manifest 2 + data-theme-selector-location 9 +
  color-no-hex 2 + declaration-no-important 1; `no-unlayered-css` / `selector-max-specificity`
  = 0 (W-Layer/W-Spec done) [measured].
- Reference value: the would-be components seed if deal ever migrates legacy→components =
  **223** distinct classes (453 `@layer legacy` rules, plugin-identical `classTokens`),
  enumerated in full [measured]. Migration is not planned.
- Report: `_work/reports/2026-07-18-deal-components-allowlist-seed.md`. Relayed to
  fleet-tooling + hub. **hub verdict: (A) legacy-manifest** — deal's allowlist entry stays 0;
  register the 2 legacy files instead. No speculative 223 pre-load (registry records measured
  reality, not planned migrations) [reported: hub].

---

## CI Phase 0-1b — split status contexts (#142, PR #143, merged `f7c7220`)

Full-compliance campaign 0-1b (tightening hub's 0-1 branch-protection lock), vault shape.

- `backend-ci`: job `check` → `backend-check` (rename only). `frontend-ci`: job `check` →
  `frontend-check` **+ removed the `paths` filter** so the frontend check runs on every
  PR/push (incl. backend/docs-only), not just `frontend/**` changes.
- Resolves two known weaknesses: (1) both jobs named `check` deduped into one status context;
  (2) the frontend paths filter left `check` "Expected" on non-frontend PRs.
- **Deadlock found & documented:** with `required_status_checks.contexts = ["check"]` and
  `enforce_admins = true` (measured via `gh api`), renaming the jobs makes this PR emit the
  new green contexts while the old required `check` stays "Expected" forever →
  `mergeStateStatus = BLOCKED`, un-mergeable even for an admin [measured]. Correct order is
  **PATCH required contexts before merge**, not after (hub's initial "PATCH after merge"
  would deadlock the merge itself). hub PATCHed `["check"]` → `["backend-check",
  "frontend-check"]`; state went BLOCKED → CLEAN; self-merged `f7c7220` [measured]. hub is
  propagating this "rename ⇒ PATCH-before-merge" order to vault/contact/profile.

---

## Environment-hazard fix — vendor NENE2 realigned to lock

hub flagged (assessment layer 2) that local `vendor/hideyukimori/nene2` was **v1.10.0**
while `composer.lock` pins **v1.11.0** (7-day stale; a local phpunit run would have tested
against v1.10 — same family as the contact production Fatal). Ran `composer install`.

- `installed.json` now: `hideyukimori/nene2 = v1.11.0`, `vlucas/phpdotenv = v5.6.4`
  [measured]. `composer.lock` untouched (`install`, not `update`); `vendor/` is gitignored
  so no tracked change. CI was already fresh-install, so prior CI greens stay valid.

---

## Standing task — article-material supply

Per 施主 hide (2026-07-18), article ideas now go directly to the articles Rina (handle
`articles`/8794). Pitched two measured stories: (1) the CI-rename → required-check deadlock
above; (2) one stylelint rule governing three repos through two mechanisms (allowlist vs
legacy-manifest; deal's 0 means "different mechanism", not "non-compliant"). Both carry
primary sources (PRs / commands / the seed report) for re-verification.

---

## 📊 Numbers

- Code PRs merged: **1** (#143, `f7c7220`) [measured].
- fleet#65 seed: `layer-components-allowlist` **0**, `layer-legacy-manifest-only` **2**,
  reference class universe **223** [measured]; hub verdict **(A) legacy-manifest**.
- vendor realign: NENE2 **v1.10.0 → v1.11.0** [measured].
- Reports authored: **1** (`_work/reports/2026-07-18-deal-components-allowlist-seed.md`).
- Lane 1 (Wave G gate): still **parked** on fleet legacy-manifest tooling.
